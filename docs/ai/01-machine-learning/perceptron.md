---
layout: default
title: 퍼셉트론(Perceptron)
parent: 1. 기계학습
grand_parent: AI (인공지능)
nav_order: 14
---

# 퍼셉트론(Perceptron)
{: .fs-8 }

6. 퍼셉트론
{: .label .label-purple }

---

## 🎯 기술사 수준 설명

### 📌 핵심 암기 (Quick Reference)

{: .highlight }
> **퍼셉트론**: 인간의 신경망과 유사하게 만든 입력층, 은닉층, 출력층으로 구성되어진 학습 능력을 가진 신경망 모델
> - (유형) 단층퍼셉트론(AND, OR, NAND), 다층퍼셉트론(XOR)
> - (활성화함수) `항시알계` 항등함수, 시그모이드, ReLU, 계단함수
> - ⭐ **차별점**: 단층 vs 다층 - 단층은 XOR 처리 불가, 다층은 은닉층 추가로 XOR 처리 가능

---

## 핵심 키워드

`입력Layer` `은닉Layer` `출력Layer` `가중치` `Bias(편향)` `활성화 함수`

---

## 정의/개념

인간의 신경망과 유사하게 만든 입력층, 은닉층, 출력층으로 구성되어진 학습 능력을 가진 신경망 모델

---

## 유형

| 유형 | 설명 |
|:-----|:-----|
| **단층퍼셉트론** | AND, OR, NAND 처리 가능 |
| **다층퍼셉트론** | XOR 처리 가능, 은닉층 추가, bias(편향) |

---

## 구성요소

| 구분 | 항목 | 설명 |
|:-----|:-----|:-----|
| **입력층** | 입력Layer | 기초데이터 입력 |
| **은닉층** | 가중치 | 입력값에 곱해지는 가중치 |
| | Bias(편향) | 활성화 함수에 더해지는 값 |
| | Net Input Function | 가중치 입력값 곱 |
| | 활성화 함수 | 출력값 결정 함수 |
| | Critical Point | 활성화 최소값 |
| **출력층** | 출력Layer | 출력값 |

---

## 활성화함수 `항시알계`

| 암기 | 항목 | 수식 | 설명 |
|:-----|:-----|:-----|:-----|
| **항** | 항등함수 | f(x) = x | 선형출력 |
| **시** | 시그모이드함수 | y=1/(1+e^-x) | 0과 1사이, 0.5기준 |
| **알** | ReLU함수 | max(0, x) | 0보다 크면 x |
| **계** | 계단함수 | f(NET) = 1 if NET >= T, 0 if NET < T | 기준값 T기준 0 아니면 1 |

---

## 개념도

```
                ┌─────────────────────────────────────────────┐
                │              다층 퍼셉트론                    │
                │                                             │
   x₁ ────→ ○ ──┼──→ ○ ──────────────────────→ ○ ────→ Output
                │     ╲                     ╱                 │
   x₂ ────→ ○ ──┼──→   ○ ─────────────→ ○ ───────→            │
                │         ╲         ╱                         │
   x₃ ────→ ○ ──┼──→       ○ ─────→                           │
                │                                             │
                │  입력층    은닉층 1    은닉층 2    출력층      │
                └─────────────────────────────────────────────┘
```

---

## 연계 토픽

- [활성화 함수]({{ site.baseurl }}/docs/ai/01-machine-learning/activation-function)
- [기울기 소실]({{ site.baseurl }}/docs/ai/01-machine-learning/vanishing-gradient)
- [MLP]({{ site.baseurl }}/docs/ai/02-deep-learning/mlp)

---

## 학습 체크리스트

- [ ] 퍼셉트론 정의 암기
- [ ] 단층 vs 다층 퍼셉트론 차이 설명
- [ ] 구성요소(입력층, 은닉층, 출력층) 암기
- [ ] `항시알계` 활성화 함수 4가지 설명
